import 'package:flutter/material.dart';

class TwoFactorSettings extends StatelessWidget {
  const TwoFactorSettings({super.key});

  @override
  Widget build(BuildContext context) {
    return Scaffold();
  }
}