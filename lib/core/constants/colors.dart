import 'package:flutter/material.dart';

class AppColor {
  static const dark = Colors.black;
  static const light = Colors.white;
  static const red = Colors.redAccent;
  static const blue = Colors.lightBlue;
  static const yellow = Colors.yellowAccent;
  static const orange = Colors.deepOrangeAccent;
  static const grey = Colors.grey;
  static const darkBlue = Color(0xFF002A4F);
  static const authentication_theme_color = Color.fromRGBO(0, 42, 79, 1);
  static const authentication_button_color = Color.fromRGBO(19, 194, 194, 1);
}